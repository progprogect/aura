# План выноса лид-магнитов в личный кабинет

## Текущая ситуация

**Лид-магниты сейчас:**
- Редактируются только в режиме редактирования профиля (`/specialist/[slug]?edit=true`)
- Компонент `LeadMagnetsEditor` используется только в `SpecialistProfileWithEdit`
- Модальное окно `LeadMagnetModal` для создания/редактирования

**Услуги в личном кабинете:**
- Отображаются в `/profile` в Card "💼 Мои услуги"
- Компонент `ServicesList` с кнопкой "Добавить услугу"
- Grid layout для карточек
- Empty state с призывом к действию
- Использует `ServiceForm` для создания/редактирования

## Предлагаемое UX решение

### 1. Расположение в личном кабинете

**Вариант A: Рядом с услугами (рекомендуется)**
- Разместить Card "🎁 Бесплатные материалы" сразу после Card "💼 Мои услуги"
- Обе секции в основной колонке (lg:col-span-2)
- Логическая группировка: услуги (платные) → материалы (бесплатные)

**Вариант B: В боковой панели**
- Менее предпочтителен, так как лид-магниты требуют больше места для отображения

**Выбор: Вариант A** - рядом с услугами, в основной колонке

### 2. Структура компонента LeadMagnetsList

**Аналогично ServicesList:**
- Header с счетчиком: "X из 6 лид-магнитов"
- Кнопка "Добавить материал" (disabled при достижении лимита)
- Empty state с иконкой, заголовком, описанием и кнопкой
- Grid layout для карточек лид-магнитов
- Кнопки редактирования/удаления на каждой карточке

### 3. Отображение карточек лид-магнитов

**Мобильные устройства:**
- 1 колонка (grid-cols-1)
- Компактные карточки с:
  - Emoji (крупный, слева)
  - Заголовок (truncate)
  - Тип (badge: Файл/Ссылка/Услуга)
  - Описание (line-clamp-2)
  - Кнопки действий (редактировать/удалить)

**Десктоп:**
- 2 колонки (sm:grid-cols-2)
- Более детальные карточки с:
  - Emoji (крупный)
  - Заголовок
  - Тип (badge)
  - Описание (полное или line-clamp-3)
  - Highlights (если есть, до 2-3 пунктов)
  - Кнопки действий

**Стиль карточек:**
- Градиентный фон (from-purple-50 to-pink-50) для визуального отличия от услуг
- Border (border-purple-200)
- Hover эффект (hover:border-purple-300, hover:shadow-md)
- Анимации (framer-motion) для плавного появления

### 4. Empty State

**Аналогично услугам:**
- Центрированный блок с:
  - Иконка (Gift, 64x64, bg-purple-100)
  - Заголовок: "Создайте бесплатные материалы"
  - Описание: "Привлекайте клиентов полезными материалами: чек-листы, гайды, видео, бесплатные консультации"
  - Кнопка "Создать первый материал"

### 5. Интеграция с существующим модальным окном

**Использовать существующий LeadMagnetModal:**
- Не создавать новый компонент
- Переиспользовать `LeadMagnetModal` из `src/components/specialist/edit/LeadMagnetModal.tsx`
- Логика создания/редактирования остается той же

### 6. Удаление из редактирования профиля

**После выноса в личный кабинет:**
- Убрать секцию "Лид-магниты" из режима редактирования профиля
- Оставить только отображение для клиентов (без редактирования)
- Редактирование доступно только из личного кабинета

## Архитектура реализации

### Компоненты

1. **LeadMagnetsList.tsx** (новый)
   - Путь: `src/components/specialist/dashboard/LeadMagnetsList.tsx`
   - Аналогично `ServicesList.tsx`
   - Props: `leadMagnets: LeadMagnetUI[]`, `onRefresh: () => void`
   - Использует `LeadMagnetModal` для создания/редактирования

2. **LeadMagnetModal.tsx** (переиспользовать)
   - Уже существует в `src/components/specialist/edit/LeadMagnetModal.tsx`
   - Не требует изменений

### Интеграция

1. **Обновить `/profile/page.tsx`:**
   - Добавить загрузку `leadMagnets` в `getUserData()`
   - Добавить Card "🎁 Бесплатные материалы" после услуг
   - Передать `leadMagnets` в `LeadMagnetsList`

2. **Обновить `SpecialistProfileWithEdit.tsx`:**
   - Убрать редактирование лид-магнитов из режима редактирования
   - Оставить только отображение для клиентов

### Адаптивность

**Мобильные (< 640px):**
- 1 колонка
- Компактные карточки
- Кнопки действий всегда видны (не только при hover)

**Планшеты (640px - 1024px):**
- 2 колонки
- Средние карточки

**Десктоп (> 1024px):**
- 2 колонки (в рамках основной колонки личного кабинета)
- Детальные карточки

## Преимущества решения

1. **Консистентность:** Одинаковый UX с услугами
2. **Удобство:** Все управление контентом в одном месте (личный кабинет)
3. **Мобильная оптимизация:** Приоритет мобильным устройствам
4. **Простота:** Переиспользование существующих компонентов
5. **Масштабируемость:** Легко добавить другие типы контента

## Порядок реализации

1. Создать `LeadMagnetsList.tsx` по аналогии с `ServicesList.tsx`
2. Обновить `getUserData()` для загрузки лид-магнитов
3. Добавить Card с лид-магнитами в `/profile/page.tsx`
4. Убрать редактирование из `SpecialistProfileWithEdit.tsx`
5. Протестировать на мобильных и десктопе

